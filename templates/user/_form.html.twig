{{ form_start(form) }}
    {{ form_errors(form) }} {# Affiche les eventuelles erreurs #}
        {{ form_row(form.email) }}
        {{ form_row(form.roles) }}
        {{ form_row(form.save) }}
{{ form_end(form) }}
